//TODO - add cloud_firestore_mocks package
//import 'package:cloud_firestore_mocks/cloud_firestore_mocks.dart';
import 'package:flutter_test/flutter_test.dart';

void main() async {
  testWidgets('Add user test', (WidgetTester tester) async {
    // final instance = MockFirestoreInstance();
    // await instance.collection('user').add({
    //   'displayName': 'Bob',
    //   'email': 'bob@gmail.com',
    //   'photoURL': 'assets/images/croix-agse.png',
    // });
    // final snapshot = await instance.collection('user').get();
    // print(snapshot.docs.length);
    // print(snapshot.docs.first['displayName']);
    // print(snapshot.docs.first['email']);
    // print(snapshot.docs.first['photoURL']);
    // print(instance.dump());
  });
}
