import 'package:flutter/material.dart';

class BrowsePhotosPage extends StatelessWidget {
  const BrowsePhotosPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
