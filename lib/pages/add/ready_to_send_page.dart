import 'package:flutter/material.dart';

class ReadyToSendPage extends StatelessWidget {
  const ReadyToSendPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
